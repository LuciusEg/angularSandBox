import { MyColorDirDirective } from './my-color-dir.directive';

describe('MyColorDirDirective', () => {
  it('should create an instance', () => {
    const directive = new MyColorDirDirective();
    expect(directive).toBeTruthy();
  });
});
